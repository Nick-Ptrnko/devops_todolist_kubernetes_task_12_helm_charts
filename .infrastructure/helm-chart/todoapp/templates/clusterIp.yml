apiVersion: v1
kind: Service
metadata:
#  name: todoapp-service
#  namespace: todoapp
  name: {{ .Chart.Name }}-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
#    app: todoapp
    app: {{ .Chart.Name }}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
